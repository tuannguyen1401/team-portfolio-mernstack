<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || 'Đăng nhập' %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-950 flex items-center justify-center font-sans">
  <div class="w-full max-w-md px-4">
    <div class="bg-white/5 border border-white/10 rounded-2xl shadow-2xl shadow-indigo-900/30 backdrop-blur-lg p-8">
      <div class="flex items-center gap-4 mb-6">
        <div class="w-12 h-12 rounded-xl bg-gradient-to-tr from-indigo-500 to-indigo-700 flex items-center justify-center shadow-lg">
          <span class="text-2xl font-bold text-white">A</span>
        </div>
        <div>
          <h1 class="text-xl font-bold text-white mb-1 tracking-wide">Admin Portal</h1>
          <p class="text-sm text-slate-400">Đăng nhập để tiếp tục</p>
        </div>
      </div>
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-800/90 text-red-50 border border-red-900 rounded-lg px-4 py-2 mb-4 text-sm font-medium flex items-center gap-2">
          <svg class="w-4 h-4 text-red-200" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01M21 12A9 9 0 1 1 3 12a9 9 0 0 1 18 0Z"/></svg>
          <%= error %>
        </div>
      <% } %>
      <form method="post" action="/login" class="flex flex-col gap-5">
        <div>
          <label class="block text-slate-400 text-sm font-medium mb-2" for="username">Tên đăng nhập</label>
          <input
            class="w-full px-4 py-3 rounded-lg bg-slate-900/80 border border-slate-700 text-slate-100 placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:border-indigo-500 transition"
            id="username"
            name="username"
            type="text"
            autocomplete="username"
            placeholder="admin"
            required
          />
        </div>
        <div>
          <label class="block text-slate-400 text-sm font-medium mb-2" for="password">Mật khẩu</label>
          <input
            class="w-full px-4 py-3 rounded-lg bg-slate-900/80 border border-slate-700 text-slate-100 placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:border-indigo-500 transition"
            id="password"
            name="password"
            type="password"
            autocomplete="current-password"
            placeholder="••••••••"
            required
          />
        </div>
        <button
          class="mt-2 w-full py-3 rounded-lg bg-gradient-to-tr from-indigo-500 to-indigo-700 text-white font-semibold shadow-md hover:from-indigo-600 hover:to-indigo-800 transition active:scale-[.98]"
          type="submit"
        >
          Đăng nhập
        </button>
      </form>
      <div class="mt-6 text-center text-xs text-slate-500">
        &copy; <%= new Date().getFullYear() %> Admin Portal. All rights reserved.
      </div>
    </div>
  </div>
</body>
</html>
